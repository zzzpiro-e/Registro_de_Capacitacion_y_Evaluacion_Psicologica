// Archivo: container_perfil_psicologo_5.dart
import 'package:flutter/material.dart';

class ContainerPerfilPsicologoCinco extends StatelessWidget {
  const ContainerPerfilPsicologoCinco({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 300,
      child: Center(
        child: CircularProgressIndicator(
          color: Color(0xFF388E3C),
        ),
      ),
    );
  }
}